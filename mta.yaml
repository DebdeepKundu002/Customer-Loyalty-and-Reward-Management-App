_schema-version: 3.3.0
ID: CustomerLoyaltyApp
description: A simple CAP project.
version: 1.0.0
modules:
- name: CustomerLoyaltyApp-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: CustomerLoyaltyApp-auth
  - name: CustomerLoyaltyApp-db
  - name: CustomerLoyaltyApp-logging
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: CustomerLoyaltyApp-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: CustomerLoyaltyApp-db
  parameters:
    buildpack: nodejs_buildpack
- name: CustomerLoyaltyApp
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: CustomerLoyaltyApp-auth
  - name: CustomerLoyaltyApp-logging
  - name: CustomerLoyaltyApp-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: CustomerLoyaltyApp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: CustomerLoyaltyApp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - customerloyaltyui.zip
      name: customerloyaltyui
      target-path: resources/
    - artifacts:
      - rewardpoliciesui.zip
      name: rewardpoliciesui
      target-path: resources/
    - artifacts:
      - retailstuffui.zip
      name: retailstuffui
      target-path: resources/
- name: customerloyaltyui
  type: html5
  path: app/customerloyaltyui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: rewardpoliciesui
  type: html5
  path: app/reward_policies_ui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: retailstuffui
  type: html5
  path: app/retailstuff_ui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: CustomerLoyaltyApp-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: CustomerLoyaltyApp-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: CustomerLoyaltyApp-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: CustomerLoyaltyApp-logging
  type: org.cloudfoundry.managed-service
  parameters:
    service: application-logs
    service-plan: lite
- name: CustomerLoyaltyApp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: CustomerLoyaltyApp-html5-service
    service-plan: app-host
- name: CustomerLoyaltyApp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: CustomerLoyaltyApp-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
